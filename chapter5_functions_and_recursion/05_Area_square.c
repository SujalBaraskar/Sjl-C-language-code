#include<stdio.h>
#include<math.h>

int main(){ 
    int side;
    printf("enter the value of side");
    scanf("%d",&side);
    printf("area of square is %f",pow(side,2)); //pow function returns double value
return 0;
}